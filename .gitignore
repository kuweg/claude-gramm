/target
parser/target

# Never commit real session transcripts (may contain secrets — see DESIGN §8)
*.jsonl
!fixtures/*.jsonl

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
dist/
*.egg-info/
.ruff_cache/

# Secrets — never commit a filled-in .env (the template .env.example is tracked)
.env

# Local state
state.db
